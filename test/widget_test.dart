import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Skipped — app requires camera, Hive and secure storage init before running
    expect(true, isTrue);
  });
}
